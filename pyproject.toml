[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "digital-twin"
version = "0.1.0"
description = "Runnable digital twin prototype"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Digital Twin Team"}]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
digital-twin = "digital_twin.cli:main"
